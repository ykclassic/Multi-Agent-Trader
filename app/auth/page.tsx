import {signIn,signUp} from "./actions";
export default function AuthPage(){return <main className="container" style={{paddingTop:72,maxWidth:520}}><div className="card"><h1>Multi-Agent Trader</h1><p className="muted">Foundation: authenticated workspace and strategy control plane.</p><form action={signIn} className="grid" style={{marginTop:24}}><input name="email" type="email" placeholder="Email" required/><input name="password" type="password" placeholder="Password" required minLength={8}/><button type="submit">Sign in</button></form><form action={signUp} style={{marginTop:12}}><input name="email" type="email" placeholder="New account email" required/><input name="password" type="password" placeholder="New account password" required minLength={8} style={{marginTop:8}}/><button type="submit" style={{marginTop:8}}>Create account</button></form></div></main>}