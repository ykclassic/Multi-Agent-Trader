import {redirect} from "next/navigation";import {createSupabaseServerClient} from "@/lib/supabase/server";import {createStrategy,createStrategyVersion} from "./actions";
export default async function StrategiesPage(){const s=await createSupabaseServerClient();const {data:{user}}=await s.auth.getUser();if(!user)redirect("/auth");const {data:strategies}=await s.from("strategies").select("id,name,status,strategy_versions(version,created_at)").order("created_at",{ascending:false});return <main className="container"><nav><strong>Strategies</strong><a href="/dashboard">Dashboard</a></nav><section><h1>Strategy Registry</h1><p className="muted">Declarative, versioned strategy contracts. Foundation mode does not execute orders.</p></section><section className="card"><h2>Create strategy</h2><form action={createStrategy} style={{display:"flex",gap:8}}><input name="name" placeholder="e.g. FX Macro Research" required/><button type="submit">Create</button></form></section><section className="grid">{(strategies??[]).map((s:any)=><div className="card" key={s.id}><h3>{s.name} <span className="pill">{s.status}</span></h3><p className="muted">Versions: {(s.strategy_versions??[]).map((v:any)=>v.version).join(", ")||"none"}</p><form action={createStrategyVersion}><input type="hidden" name="strategy_id" value={s.id}/><button type="submit">Create next version</button></form></div>)}</section></main>}